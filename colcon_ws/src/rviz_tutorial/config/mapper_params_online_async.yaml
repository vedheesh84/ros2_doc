slam_toolbox:
  ros__parameters:

    # ============================================================
    # Solver Plugin Settings
    # ============================================================
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # ============================================================
    # Core TF / Topic Configuration
    # ============================================================
    odom_frame: odom
    map_frame: map
    base_frame: base_link
    scan_topic: /scan
    use_map_saver: true

    # Mode: "mapping" = build map, "localization" = use map
    mode: mapping

    # ============================================================
    # Timing & Transform Parameters
    # ============================================================
    debug_logging: false
    throttle_scans: 1                    # process every scan
    transform_publish_period: 0.05       # more stable TF update rate
    map_update_interval: 2.0             # faster map refresh for small environments
    resolution: 0.05                     # good tradeoff for LIDAR accuracy and speed
    min_laser_range: 0.3                 # match your LIDAR’s minimum (avoid warnings)
    max_laser_range: 12.0                # match your LIDAR’s maximum
    minimum_time_interval: 0.2           # handle faster robot motion
    transform_timeout: 0.5
    tf_buffer_duration: 60.0
    stack_size_to_use: 40000000
    enable_interactive_mode: true

    # ============================================================
    # Scan Matching / Loop Closure
    # ============================================================
    use_scan_matching: true
    use_scan_barycenter: true
    minimum_travel_distance: 0.2          # update map sooner when robot moves
    minimum_travel_heading: 0.1           # smaller heading threshold
    scan_buffer_size: 10
    scan_buffer_maximum_scan_distance: 10.0

    # Fine-tuned for small worlds
    link_match_minimum_response_fine: 0.1
    link_scan_maximum_distance: 1.5
    loop_search_maximum_distance: 3.0
    do_loop_closing: true
    loop_match_minimum_chain_size: 10
    loop_match_maximum_variance_coarse: 2.0
    loop_match_minimum_response_coarse: 0.3
    loop_match_minimum_response_fine: 0.4

    # ============================================================
    # Correlation Parameters
    # ============================================================
    correlation_search_space_dimension: 0.5
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1

    # ============================================================
    # Loop Closure Parameters
    # ============================================================
    loop_search_space_dimension: 6.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03

    # ============================================================
    # Scan Matcher Parameters
    # ============================================================
    distance_variance_penalty: 0.4
    angle_variance_penalty: 1.0
    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.1745    # 10 degrees for better speed
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true
    min_pass_through: 2
    occupancy_threshold: 0.25             # higher = cleaner map

